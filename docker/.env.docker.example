# =============================================================================
# Docker Environment Variables Template
# =============================================================================
# Copy this file to .env and configure your settings
# Usage: docker-compose --env-file .env up -d
# =============================================================================

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Bybit API Configuration
# -----------------------------------------------------------------------------
# Get your API credentials from: https://www.bybit.com/app/user/api-management

# Your Bybit API Key
BYBIT_API_KEY=your_api_key_here

# Your Bybit API Secret
BYBIT_API_SECRET=your_api_secret_here

# Bybit Environment Selection
# Options:
#   - testnet        : Fake money, test environment (safest for development)
#   - demo_mainnet   : Fake money, real market prices (recommended for testing)
#   - live_mainnet   : REAL MONEY, live trading (use with extreme caution!)
BYBIT_ENVIRONMENT=demo_mainnet

# Legacy settings (keep for backward compatibility)
BYBIT_TESTNET=false
BYBIT_DEMO=true
BYBIT_BASE_URL=https://api.bybit.com

# -----------------------------------------------------------------------------
# Trading Configuration
# -----------------------------------------------------------------------------
# Risk amount per trade (in dollars or percentage)
RISK_PER_TRADE=100.0

# Maximum leverage to use (1-100x, recommended: 10x or less)
MAX_LEVERAGE=10

# Minimum risk-reward ratio for trades
RISK_REWARD_RATIO=3.0

# -----------------------------------------------------------------------------
# ICT Monitor Configuration
# -----------------------------------------------------------------------------
# Port for ICT Enhanced Monitor web interface
ICT_MONITOR_PORT=5001

# Host binding (0.0.0.0 for Docker, localhost for local)
ICT_MONITOR_HOST=0.0.0.0

# -----------------------------------------------------------------------------
# Signal Generation Parameters
# -----------------------------------------------------------------------------
# Base probability for signal generation (0.0-1.0)
SIGNAL_PROBABILITY_BASE=0.035

# Minimum confluence threshold for valid signals (0.0-1.0)
CONFLUENCE_THRESHOLD=0.35

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Path to SQLite database (will be created if doesn't exist)
DATABASE_PATH=/app/data/trading.db

# -----------------------------------------------------------------------------
# Notification Settings (Optional)
# -----------------------------------------------------------------------------
# Email notifications (SMTP)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
# ALERT_EMAIL=your_alert_email@gmail.com

# Telegram notifications (optional)
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token
# TELEGRAM_CHAT_ID=your_telegram_chat_id

# -----------------------------------------------------------------------------
# Advanced Configuration (Optional)
# -----------------------------------------------------------------------------
# Python optimization flags
# PYTHONOPTIMIZE=1

# Timezone (default: UTC)
# TZ=UTC

# Resource limits (for docker-compose)
# MEMORY_LIMIT=2G
# CPU_LIMIT=2.0
